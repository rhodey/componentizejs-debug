package local:app;

interface helpers-interface {
  poll: func(handle: s64) -> string;
  chat-completion: func(api-key: string, json: string) -> s64;
}

world helpers {
  export helpers-interface;
}

world app {
  import helpers-interface;
}
